# 新闻评论功能使用说明

## 功能概述
本次更新为在线教育网站添加了新闻评论功能，支持用户登录状态下对新闻进行评论和回复。

## 新增文件
### 后端文件
1. `Community_education/src/main/java/com/example/community_education/Model/NewsComment.java` - 评论实体类
2. `Community_education/src/main/java/com/example/community_education/Dao/NewsCommentMapper.java` - 评论数据访问接口
3. `Community_education/src/main/java/com/example/community_education/Service/NewsCommentService.java` - 评论服务接口
4. `Community_education/src/main/java/com/example/community_education/Service/impl/NewsCommentImpl.java` - 评论服务实现类
5. `Community_education/src/main/resources/mapper/NewsCommentMapper.xml` - MyBatis映射文件
6. `Community_education/src/main/resources/sql/news_comment.sql` - 数据库表创建脚本

### 前端文件
1. `face_education/src/components/MainBody/NewsComment.vue` - 评论组件

### 修改的文件
1. `Community_education/src/main/java/com/example/community_education/Controller/publicController/PublicController.java` - 添加评论相关API
2. `face_education/src/components/MainBody/NewText.vue` - 集成评论组件

## 启动步骤

### 1. 数据库准备
首先需要创建评论表，执行以下SQL脚本：
```sql
-- 使用你的数据库
USE your_database_name;

-- 执行创建表的SQL
SOURCE /path/to/Community_education/src/main/resources/sql/news_comment.sql;
```

### 2. 后端启动
```bash
# 进入后端项目目录
cd Community_education

# 使用Maven启动项目
mvn spring-boot:run
```

或者使用IDE直接运行`Community_educationApplication.java`主类。

### 3. 前端启动
```bash
# 进入前端项目目录
cd face_education

# 安装依赖（如果尚未安装）
npm install

# 启动开发服务器
npm run serve
```

## 功能验证步骤

### 1. 访问网站
打开浏览器，访问前端启动后的地址（通常是 http://localhost:8080）

### 2. 登录系统
- 点击网站上的登录按钮
- 输入用户名和密码进行登录
- 确保登录状态正常

### 3. 查看新闻
- 导航到"新闻公告"页面
- 点击任意一条新闻进入详情页

### 4. 测试评论功能
1. **发表评论**
   - 在新闻详情页下方，找到评论区域
   - 在评论输入框中输入评论内容
   - 点击"发表评论"按钮
   - 验证评论是否显示在评论列表中

2. **回复评论**
   - 点击某条评论下方的"回复"按钮
   - 在回复输入框中输入回复内容
   - 点击"回复"按钮
   - 验证回复是否显示在该评论下方，并包含@用户名

3. **删除评论**
   - 找到自己发表的评论或回复
   - 点击评论右上角的"删除"按钮
   - 在确认对话框中点击"确定"
   - 验证评论是否已被删除

4. **未登录状态测试**
   - 退出登录状态
   - 刷新新闻详情页
   - 验证是否显示"请先登录后再发表评论"的提示
   - 验证评论输入框是否不可见

## API接口说明

### 1. 获取评论列表
- 请求路径：`POST /public/comment/list`
- 请求参数：`{newsId: 新闻ID}`
- 响应数据：评论列表

### 2. 添加评论
- 请求路径：`POST /public/comment/add`
- 请求参数：`{newsId: 新闻ID, content: 评论内容, parentId: 父评论ID, parentUsername: 被回复用户名}`
- 响应数据：操作结果

### 3. 删除评论
- 请求路径：`POST /public/comment/delete`
- 请求参数：`{id: 评论ID}`
- 响应数据：操作结果

## 注意事项
1. 确保数据库连接配置正确
2. 确保用户登录状态正常，评论功能需要用户登录
3. 评论内容长度限制为500字符，回复内容限制为300字符
4. 用户只能删除自己的评论
5. 删除评论采用软删除方式，不会真正从数据库中删除记录

## 常见问题
1. **评论不显示**：检查用户是否已登录，检查网络请求是否正常
2. **删除失败**：确认是删除自己的评论，检查网络请求是否正常
3. **数据库错误**：确认数据库表是否已创建，确认数据库连接配置是否正确
4. **"没有找到服务，请确认请求地址是否正确"错误**：
   - 检查后端服务是否正常启动
   - 确认前端请求地址是否正确（默认为http://localhost:8700）
   - 检查后端API路径是否与前端请求路径匹配
5. **"发表评论失败，请稍后重试"错误**：
   - 确认用户已登录
   - 检查token是否正确传递
   - 查看后端日志，确认是否有具体错误信息