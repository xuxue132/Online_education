# 新闻评论功能实现说明

## 一、功能概述
本实现在线教育网站的新闻评论功能，支持：
- 一级评论（直接评论新闻）
- 二级评论（回复其他评论，带@用户）
- 删除自己的评论
- 显示用户名和评论时间

## 二、数据库设计
```sql
create table comment
(
    id          int auto_increment
        primary key,
    news_id     int          not null comment '新闻ID',
    user_id     int          not null comment '用户ID',
    content     text         not null comment '评论内容',
    parent_id   int default 0 null comment '父评论ID，0表示一级评论',
    reply_to_id int default 0 null comment '回复的用户ID',
    create_time timestamp    null comment '创建时间',
    deletes     int default 0 null comment '假删除标记',
    INDEX `idx_news_id` (`news_id`),
    INDEX `idx_parent_id` (`parent_id`),
    INDEX `idx_user_id` (`user_id`)
)
comment '新闻评论表';
```

## 三、后端实现

### 1. 实体类
- `Comment.java` - 评论实体，包含评论的基本信息和关联用户信息

### 2. 数据访问层
- `CommentMapper.java` - MyBatis接口，定义评论的增删改查方法
- `CommentMapper.xml` - SQL映射文件
- `UserMapper.java/xml` - 添加了根据手机号查询用户的方法

### 3. 业务逻辑层
- `CommentService.java` - 评论服务接口
- `CommentServiceImpl.java` - 评论服务实现，包含：
  - Token验证
  - 用户权限检查
  - 评论的增删查操作

### 4. 控制层
- `PublicController.java` - 添加了三个接口：
  - `/AddComment` - 添加评论
  - `/GetComments` - 获取新闻评论
  - `/DeleteComment` - 删除评论

## 四、前端实现

### 1. 组件
- `NewText.vue` - 新闻详情页，添加了完整的评论区：
  - 评论输入框
  - 评论列表展示
  - 回复功能
  - 删除按钮
  - 时间格式化显示

### 2. 样式
- 添加了评论区的完整CSS样式，包括：
  - 评论输入框样式
  - 评论列表样式
  - 回复框样式
  - 按钮样式

## 五、启动项目

### 后端启动
```bash
cd /Users/xuxue/Documents/11823020317_徐学的副本/Money/Online_education/Community_education
mvn spring-boot:run
```

### 前端启动
```bash
cd /Users/xuxue/Documents/11823020317_徐学的副本/Money/Online_education/face_education
npm run serve
```

## 六、功能验证

### 1. 准备工作
1. 确保MySQL数据库已启动
2. 执行script.sql创建comment表
3. 启动后端服务（默认端口8080）
4. 启动前端服务（默认端口8100）

### 2. 验证步骤

#### 步骤1：登录系统
- 打开浏览器访问：http://localhost:8100/
- 点击"登录"按钮，输入用户名和密码登录

#### 步骤2：查看新闻详情
- 登录成功后，点击"新闻公告"或"新闻列表"
- 选择任意一条新闻，进入新闻详情页
- 页面底部会显示评论区

#### 步骤3：发表一级评论
- 在评论输入框中输入评论内容
- 点击"发表评论"按钮
- 验证：评论成功后会显示在评论列表中，显示用户名、时间和内容

#### 步骤4：回复评论
- 在任意评论右侧点击"回复"按钮
- 在回复输入框中输入回复内容（会自动@被回复用户）
- 点击"回复"按钮
- 验证：回复成功后会显示在原评论下方，显示@用户信息

#### 步骤5：删除评论
- 找到自己发表的评论（会显示"删除"按钮）
- 点击"删除"按钮，确认删除
- 验证：评论从列表中消失

#### 步骤6：测试权限控制
- 使用另一个账号登录
- 尝试删除其他用户的评论
- 验证：无法删除其他用户的评论，会收到提示

#### 步骤7：未登录测试
- 退出登录状态
- 尝试发表评论
- 验证：无法发表评论，会提示"未登录，请先登录"

## 七、技术要点

1. **权限控制**：使用JWT Token验证用户身份，确保只有登录用户可评论，且只能删除自己的评论

2. **数据关联**：通过user_id关联用户表，查询评论时同时获取用户名

3. **父子评论结构**：使用parent_id区分一级评论和二级评论，reply_to_id记录被回复的用户

4. **假删除机制**：使用deletes字段标记删除状态，支持数据恢复

5. **时间格式化**：前端对评论时间进行本地化显示，提升用户体验

## 八、注意事项

1. 确保前后端服务都已启动，且数据库连接正常
2. 前端请求会自动携带Token，确保用户已登录
3. 评论内容支持基本的文本输入，无特殊字符限制
4. 回复功能会自动@被回复用户，无需手动输入
5. 所有操作都有相应的错误提示，方便用户了解操作结果

## 九、文件修改清单

### 新增文件
- Community_education/src/main/java/com/example/community_education/Model/Comment.java
- Community_education/src/main/java/com/example/community_education/Dao/CommentMapper.java
- Community_education/src/main/resources/mapper/CommentMapper.xml
- Community_education/src/main/java/com/example/community_education/Service/CommentService.java
- Community_education/src/main/java/com/example/community_education/Service/impl/CommentServiceImpl.java

### 修改文件
- script.sql - 添加comment表
- Community_education/src/main/java/com/example/community_education/Dao/UserMapper.java - 添加selectByTelephone方法
- Community_education/src/main/resources/mapper/UserMapper.xml - 添加selectByTelephone的SQL
- Community_education/src/main/java/com/example/community_education/Controller/publicController/PublicController.java - 添加评论相关接口
- face_education/src/components/MainBody/NewText.vue - 添加评论区UI和交互逻辑

## 十、后续优化建议

1. **评论分页**：当评论数量较多时，可添加分页功能
2. **评论点赞**：增加点赞功能，提升互动性
3. **评论编辑**：允许用户编辑自己的评论
4. **评论通知**：当用户的评论被回复时，发送通知
5. **富文本支持**：支持评论内容的富文本编辑
6. **评论排序**：支持按时间、热度等排序
7. **评论审核**：添加评论审核机制，过滤不当内容
