<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.community_education.Dao.FileinformationMapper" >
  <resultMap id="BaseResultMap" type="com.example.community_education.Model.Fileinformation" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="title" property="title" jdbcType="VARCHAR" />
    <result column="dates" property="dates" jdbcType="TIMESTAMP" />
    <result column="deletes" property="deletes" jdbcType="INTEGER" />
  </resultMap>

  <sql id="Base_Column_List" >
    id, title, dates, deletes
  </sql>


  <select id="selectFileInformation" resultMap="BaseResultMap" parameterType="map">
    select
    <include refid="Base_Column_List" />
    from FileInformation
    where deletes = 0
    order by dates DESC
    limit #{NowPage,jdbcType=INTEGER},10
  </select>
  <select id="selectFileInformations" resultMap="BaseResultMap" parameterType="map">
    select
    <include refid="Base_Column_List" />
    from FileInformation
    where deletes = 0 and
    (
    title LIKE #{title,jdbcType=VARCHAR}
    )
    order by dates DESC
  </select>

  <select id="selectAnFileInformation" resultMap="BaseResultMap" parameterType="map">
    select
    <include refid="Base_Column_List" />
    from FileInformation
    where deletes = 0 and id = #{id,jdbcType=INTEGER}
  </select>

  <select id="SumFileInformation" resultType="java.lang.Integer" parameterType="map">
    select
    COUNT(*)
    from FileInformation
    where deletes = 0
  </select>


  <insert id="AddFileInformations" parameterType="map" >
    insert into FileInformation (title, dates
      )
    values ( #{title,jdbcType=VARCHAR}, #{dates,jdbcType=TIMESTAMP}
      )
  </insert>

  <update id="UpdateFileInformations" parameterType="map" >
    update FileInformation
    set
    title = #{title,jdbcType=VARCHAR}
    where id = #{id,jdbcType=INTEGER}
  </update>

  <update id="DeleteFileName" parameterType="map" >
    update FileInformation
    set
    deletes = 1
    where id = #{id,jdbcType=INTEGER}
  </update>
</mapper>