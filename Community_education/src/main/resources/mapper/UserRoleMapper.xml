<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.community_education.Dao.UserRoleMapper" >
  <resultMap id="BaseResultMap" type="com.example.community_education.Model.UserRole" >
    <id column="ID" property="id" jdbcType="INTEGER" />
    <result column="telephone" property="telephone" jdbcType="VARCHAR" />
    <result column="roleName" property="roleName" jdbcType="VARCHAR" />
    <result column="deleted" property="deleted" jdbcType="INTEGER" />
  </resultMap>

  <sql id="Base_Column_List" >
    user_role.ID, user_role.telephone, user_role.roleName, user_role.deleted
  </sql>

  <insert id="addUserRole" parameterType="map">
    insert into  user_role(telephone,roleName) values (#{telephone,jdbcType=VARCHAR},
                                   #{roleName,jdbcType=VARCHAR})

  </insert>

  <update id="ChangeTelPhone" parameterType="map">
    update user_role
    set
      telephone = #{NewTelephone,jdbcType=VARCHAR}
    where telephone = #{telephone,jdbcType=VARCHAR}
  </update>

  <update id="DeleteUser" parameterType="map">
    update user_role
    set
      deleted = 1
    where telephone = #{telephone,jdbcType=VARCHAR}
  </update>
  <update id="UnDeleteUser" parameterType="map">
    update user_role
    set
      deleted = 0
    where telephone = #{telephone,jdbcType=VARCHAR}
  </update>

</mapper>