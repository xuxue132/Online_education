<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.community_education.Dao.CommentMapper">
    <resultMap id="BaseResultMap" type="com.example.community_education.Model.Comment">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="news_id" jdbcType="INTEGER" property="newsId"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="content" jdbcType="LONGVARCHAR" property="content"/>
        <result column="parent_id" jdbcType="INTEGER" property="parentId"/>
        <result column="reply_to_id" jdbcType="INTEGER" property="replyToId"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="deletes" jdbcType="INTEGER" property="deletes"/>
    </resultMap>

    <resultMap id="CommentWithUserResultMap" type="com.example.community_education.Model.Comment" extends="BaseResultMap">
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="reply_username" jdbcType="VARCHAR" property="replyUsername"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, news_id, user_id, content, parent_id, reply_to_id, create_time, deletes
    </sql>

    <select id="selectByNewsId" resultMap="CommentWithUserResultMap">
        SELECT c.*, u.username, ru.username as reply_username
        FROM comment c
        LEFT JOIN user_inf u ON c.user_id = u.id
        LEFT JOIN user_inf ru ON c.reply_to_id = ru.id
        WHERE c.news_id = #{newsId}
        AND c.parent_id = 0
        AND c.deletes = 0
        ORDER BY c.create_time DESC
    </select>

    <select id="selectByParentId" resultMap="CommentWithUserResultMap">
        SELECT c.*, u.username, ru.username as reply_username
        FROM comment c
        LEFT JOIN user_inf u ON c.user_id = u.id
        LEFT JOIN user_inf ru ON c.reply_to_id = ru.id
        WHERE c.parent_id = #{parentId}
        AND c.deletes = 0
        ORDER BY c.create_time ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM comment
        WHERE id = #{id}
    </select>

    <delete id="deleteById">
        UPDATE comment SET deletes = 1 WHERE id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.community_education.Model.Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (news_id, user_id, content, parent_id, reply_to_id, create_time, deletes)
        VALUES (#{newsId}, #{userId}, #{content}, #{parentId}, #{replyToId}, #{createTime}, #{deletes})
    </insert>

    <insert id="insertSelective" parameterType="com.example.community_education.Model.Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="newsId != null">news_id,</if>
            <if test="userId != null">user_id,</if>
            <if test="content != null">content,</if>
            <if test="parentId != null">parent_id,</if>
            <if test="replyToId != null">reply_to_id,</if>
            <if test="createTime != null">create_time,</if>
            <if test="deletes != null">deletes,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="newsId != null">#{newsId},</if>
            <if test="userId != null">#{userId},</if>
            <if test="content != null">#{content},</if>
            <if test="parentId != null">#{parentId},</if>
            <if test="replyToId != null">#{replyToId},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="deletes != null">#{deletes},</if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.example.community_education.Model.Comment">
        UPDATE comment
        <set>
            <if test="newsId != null">news_id = #{newsId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="content != null">content = #{content},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="replyToId != null">reply_to_id = #{replyToId},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="deletes != null">deletes = #{deletes},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>